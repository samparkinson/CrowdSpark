<Page
    x:Class="CrowdSpark.App.Views.ProjectPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CrowdSpark.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <SplitView x:Name="MainSplitView" Background="Gray" DisplayMode="CompactInline"  IsPaneOpen="False" CompactPaneLength="50" OpenPaneLength="200">
            <SplitView.Pane>
            <Grid>
                <Grid.RowDefinitions>
                    <!--For menu options-->
                    <RowDefinition Height="*"/>
                    <!--For settings option-->
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Background="Gray" Grid.Row="0">
                    <Button x:Name="HamburgerButton" FontFamily="Segoe MDL2 Assets" Content="&#xE700;"
                     Width="50" Height="50" Background="Transparent" Click="HamburgerButton_Click"/>
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="ProfileButton" FontFamily="Segoe MDL2 Assets" Content="&#xE825;" Width="50" Height="50" Background="Transparent"/>
                        <TextBlock Text="Profile" FontSize="18" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="ProjectsButton" FontFamily="Segoe MDL2 Assets" Content="&#xE10F;" Width="50" Height="50" Background="Transparent" />
                        <TextBlock Text="Projects" FontSize="18" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="SettingsButton" FontFamily="Segoe MDL2 Assets" Content="&#xE10F;" Width="50" Height="50" Background="Transparent" />
                        <TextBlock Text="Settings" FontSize="18" VerticalAlignment="Center" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Background="Gray" Grid.Row="1">
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="LogoutButton" FontFamily="Segoe MDL2 Assets" Content="&#xE10F;" Width="50" Height="50" Background="Transparent" />
                        <TextBlock Text="{Binding LogStatusText}" FontSize="18" VerticalAlignment="Center" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </SplitView.Pane>

        <SplitView.Content>
            <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <Grid.RowDefinitions>
                    <!--For the app bar-->
                    <RowDefinition Height="80"/>
                    <!--For page contents-->
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="1" Background="#FF747474" HorizontalAlignment="Stretch" Margin="10,10" VerticalAlignment="Stretch" >
                    <Grid>
                        <Grid.RowDefinitions>
                            <!--For Post details-->
                            <RowDefinition Height="*"/>
                            <!--Location-->
                            <RowDefinition Height="*" />
                            <!--Participants-->
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0">
                            <Grid>
                                <TextBlock Margin="25" Text="{Binding Title}" FontSize="40" VerticalAlignment="Top" />
                            </Grid>

                            <Grid>
                                <TextBlock Margin="25" Text="{Binding Description}" FontSize="24" VerticalAlignment="Bottom" />
                            </Grid>
                        </StackPanel>

                        <StackPanel Grid.Row="2" DataContext="{Binding Location}">
                            <Grid>
                                <TextBlock Margin="25" Text="{Binding City}" FontSize="40" VerticalAlignment="Top" />
                            </Grid>

                            <Grid>
                                <TextBlock Margin="25" Text="{Binding Country}" FontSize="24" VerticalAlignment="Top" />
                            </Grid>
                        </StackPanel>

                        <StackPanel Grid.Row="1">
                            <Image x:Name="CountryFlagImage" Source="{Binding CountryFlag}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Grid>
        </SplitView.Content>
        </SplitView>
</Page>
